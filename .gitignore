# Build
.build/
.swiftpm/
*.xcodeproj
xcuserdata/
DerivedData/
*.app/
# wine-crossover source build staging (1.5GB+, never commit).
# NOTE: gitignore has no inline comments, so the pattern lives on its own line —
# the old "build/   # comment" form silently ignored nothing.
build/

# macOS
.DS_Store

# Artifact / scratch / personal folders
vendor/
dist/
misc/

# Loose artifacts + personal files dropped in the repo root
*.tar.xz
*.tar.gz
*.dmg
*.zip
*.iso
*.docx
*.pptx
*.pdf
*.xlsx
*.JPG
*.jpg
Htz.eVisitor*
/*.png
/*.jpeg
/*.webp
kit-payload/
Friend-Kit-*

# Downloaded Windows installers (user support artifacts — never commit)
*.exe
BsgLauncher*.exe
NDP*.exe

# Private (local only)
BUSINESS_PLAN.md
private-eft-kit/
Temp/
*.mcd

# Permanently local scratch: loose files that land in the repo root (personal
# docs, installers, media, licenses). Never committed, never published — drop
# anything here rather than leaving it loose. Cleaned out of the root
# 2026-08-04.
_local/
